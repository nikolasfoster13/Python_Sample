# Grocery List
Suppose that you’re in the habit of making a list of items you need from the grocery store.

grocery.py is a program that prompts the user for items, one per line, until the user inputs ctrl+d. Then, the program outputs the user's grocery list in all uppercase, sorted alphabetically by item, prefixing each line with the number of times that item was inputted.